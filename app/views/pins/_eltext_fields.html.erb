
<fieldset>
  <%= f.label :text, "text" %><br />
  <%= f.text_area :text %><br />
  <%= f.check_box :_destroy %>
  <%= f.label :_destroy, "Remove text" %>
  <%= f.fields_for :pincsses do |builder| %>
    <%= render 'pincss_fields', f: builder %>
  <% end %>
  <%= link_to_add_fields "Add pincsses", f, :pincsses %>
</fieldset>
